server {
    listen 80;
    server_name localhost;

    # Serve the UI (no API proxy - frontend calls API directly)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
